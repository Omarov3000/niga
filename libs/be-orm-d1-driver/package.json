{
  "name": "@w/be-orm-d1-driver",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "tsc": "tsc",
    "tsc:watch": "tsc -w",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "pnpm tsc && pnpm test",
    "lint1": "biome lint --write --unsafe --error-on-warnings src"
  },
  "dependencies": {
    "@w/orm": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.9.7",
    "@cloudflare/workers-types": "^4.20250926.0",
    "vitest": "^3.2.4"
  }
}
