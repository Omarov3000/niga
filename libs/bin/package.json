{
  "name": "@w/bin",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "tsc": "tsc",
    "tsc:watch": "tsc -w",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "pnpm tsc && pnpm test",
    "lint1": "biome lint --write --unsafe --error-on-warnings src"
  },
  "dependencies": {
    "sql-parser-cst": "^0.33.1",
    "nanoid": "^5.1.5"
  },
  "devDependencies": {
    "oxlint": "^1.13.0",
    "dedent": "^1.5.3"
  }
}
